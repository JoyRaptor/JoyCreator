<!-- Mask preset RECT: wider than tall, hard corners — the shape that differs from SQUARE
     only in proportion, so the glyph has to differ in proportion too. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp" android:height="24dp"
    android:viewportWidth="24" android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">
    <path android:fillColor="@android:color/white" android:fillType="evenOdd"
        android:pathData="M3,7h18v10h-18zM4.6,8.6v6.8h14.8v-6.8z"/>
</vector>
