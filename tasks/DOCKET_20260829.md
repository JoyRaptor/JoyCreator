# DOCKET — everything on the board as of 2026-08-29 03:00

Written for JoyRaptor, who asked: *"how many features did we invent and have on the docket
and how many are left to do?"* This is the ledger. It supersedes the "Open threads"
section of `HANDOFF_20260828.md`.

**Rule for reading this:** a row is only ✅ when someone has SEEN it work on a screen.
"Committed", "compiles" and "typechecks" are not the same thing as "works", and this repo
has been burned by that distinction three times.

---

## 1. Scoreboard

| | Count |
|---|---|
| Features designed this session | **14** |
| Shipped and verified on a device | **1** |
| Shipped, compiles, nobody has looked at it | **9** |
| Built but not yet wired into the app | **2** |
| Designed, not yet built | **2** |
| Inherited from 2026-08-28, still unverified | **5** |

**So: the building is well ahead of the looking.** That gap is the single biggest risk on
the board, and it is why §4 exists.

---

## 2. This session's work

### Landed AND verified on device ✅

| Feature | Evidence |
|---|---|
| **Audio layer sync** — pre-roll, self-calibrating drift lock, no start delay | Note 9: 1 AudioTrack started, 0 created during playback (was ~20/sec), 0 reparks (was every tick). JoyRaptor: "sounded much better" |

Four separate defects were found and fixed inside this one feature, three of them
introduced by the fix itself:

1. Layers silenced outright by a mid-playback park (`1049937f`)
2. Latency compensation written into the shared model, corrupting every reader (`5e96b5c3`)
3. The drift lock "correcting" the audio pipeline's own ~125ms latency, pausing and
   restarting every layer ~20×/second — the stutter and volume pumping JoyRaptor reported
   (`6bdf7323`)
4. Start retried every tick, spawning ~20 AudioTracks/second (`6f1d7fa1`)

### Landed, compiles, NOBODY HAS LOOKED AT IT 👀

| Feature | Commit | What to look at |
|---|---|---|
| Keyframe shape language (5 glyphs drawn from the curve) | `88e73dc3` | **ramp direction** — most likely thing to be backwards |
| A/V Sync calibration tab (click track + slider) | in `5e96b5c3` line | does the slider move the playhead against the waveform |
| Caption layers — model, storage, export, preview | staged | 3 tracks rendering independently |
| Image `presetOwned` amber keyframes | `1bc9a273` | amber appears; timeline drag converts; one undo restores |
| Fit / Fill quick actions | `1bc9a273` | tall + wide image, both modes |
| Image opacity fade handles (shares audio fade code) | `1bc9a273` | fade multiplies rather than replaces |
| Image animation presets (pan / zoom / slide) | `1bc9a273` | no background peek at any point |
| Preview perf — content-keyed cache + texture quads | staged | animated text under a blend appears at all |
| Sticky bookend keyframes follow a trim | `1bc9a273` | drag the out point, amber key follows |

### Built, not yet wired into the app 🔌

| Piece | State |
|---|---|
| `PcmSidecar` — flat memory-mapped audio bake (~2.6 MB/min) | committed `b74c57db` |
| `ScrubEngine` — AE-style granular scrubbing | committed `b74c57db` |
| `OnsetDetector` — word-start detection + snapping | committed `0eebaee2`, **16/16 harness tests pass** |

Deliberately unwired: two lanes were inside `FaditorEditorActivity` and a third freehand
editor in a 36,000-line file is how work gets lost. Wiring is a small change once the
caption lane is idle.

### Designed, not built 📐

| Feature | Where the design lives |
|---|---|
| **Word Sync mode** — modal word-scrubbing, ripple drag, TT/Tt/tt/B/U/I | designed in conversation, no spec yet |
| GL migration of captions + visualizer | `FINDING_20260829_GL_ANIMATED_GAP.md` §4 |

---

## 3. Inherited and still unverified (from 2026-08-28)

All committed and installed on 2026-08-28; none seen.

- Opacity keyframe delete + on-key dot + amber indicator — `6ac64156`
- Caption font `+ Import` chip moved first — `90d77721`
- Caption Fit tab (OFF / UNIFORM / PER_CUE) — `a9c67386`
- Export GL frames (Surface decode) — `7727af0a` `7f8c283a`, **§5 timing/PSNR never run**
- Transcript source affordance — `60919802`

---

## 4. What is actually left to do

### Blocking, in order

1. **Look at all of it.** `SPEC_20260829_DEVICE_VERIFY_ALL` — 9 unlooked-at features from
   today plus 5 from yesterday. Highest value on the board by a distance.
2. **Finish `CAPTION_LAYERS`** — drawer track list, pinch-to-size, full drawer retarget.
   Blocks `IMAGE_ANIM_PRESETS` phase 3.
3. **Finish `PREVIEW_PERF`** — staged, needs a green build and its §5 measurements.
4. **`IMAGE_ANIM_PRESETS` phase 3** — the drawer UI. Blocked on (2).

### Wanted, specced

- `SPEC_20260829_QUICK_WINS` — image-as-overlay promoted to the toolbox; video thumbnails
  in the file picker.

### Wanted, no spec yet

| Item | Why it matters |
|---|---|
| **Word Sync mode** | The reason the scrub stack exists. Fabián/Claude to spec |
| **Media consolidation / project bundling** | Projects are not portable; imported fonts die on uninstall |
| **Media-browser pin** | Untouched since the fork began; JoyRaptor wants it revisited with bundling |
| **Per-word rich text in a caption cue** | Superseded for JoyRaptor by caption layers, still the honest answer for "bold reference, plain body" |
| **Captions → GL** | Cheaper than Canvas, not more expensive. Blocked until caption layers settles |
| **Visualizer → GL** | Cheap in principle; **unmeasured**, do not act on the guess |

### Known bugs, unfixed

- **~1:03 playback ceiling** — never reproduced, no cap exists in code
- **Slide object** — parked (`fcc36551`, reverted by `ac582aa1`). **Probably obsolete:**
  caption layers may remove the need entirely. Ask JoyRaptor before resuming

---

## 5. The lesson this session keeps teaching

Every serious defect today was found by MEASURING, not by reading:

- The silence was found in `dumpsys audio` (`state:idle` while the log claimed success)
- The stutter was found in logcat (`drift desync err=-126 -> repark`, every tick)
- The AudioTrack storm was found by counting `new player piid:` lines
- The build corruption was found by listing gradle daemons (two were running)

And every one of them would have been caught in the first three seconds of the acceptance
runs the specs already asked for. **The acceptance sections are not paperwork.** Running
them is cheaper than any of the debugging above.
