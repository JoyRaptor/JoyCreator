# User feedback 2026-07-05 (real-phone hand-test on a GREEN build — first current build in a while)

## ✅ WINS
- **Sprite live-drop keyframing = delight confirmed.** User pressed play, dropped several sprite keyframes
  during playback, replayed → sprites animated at exactly the dropped timing. "Amazing and fantastic."
- **Edge auto-pan now WORKS** on the purple text drag ("did do scrolling as it neared the edges") — slice-3
  A1 confirmed live on the green build.

## 🐞 JOYRAPTOR LANE (sprites — do NOT fix here; for JoyRaptor when she resumes)
- **Two sprite keyframes can land on the EXACT SAME frame** (duplicate diamonds). User is right that this is
  useless on one layer: a same-frame drop should REPLACE the existing sprite keyframe (swap), not stack a
  second diamond. Multiple sprites at the same time belong on DIFFERENT sprite layers, not the same frame.
  FIX (JoyRaptor): in the sprite keyframe drop path, if a keyframe exists at that frame/tolerance → replace its
  sprite ref instead of inserting; keep it one undo step. (Mirrors the caption-style-keyframe replace-at-
  playhead behavior already shipped.)

## ✏️ DRAG REWRITE — device-confirmed symptoms (fold into the 🩺 SPLIT-ELEMENT fix, dragux_v3 doc)
Green-build hand-test of the purple text-item drag adds concrete symptoms to the split-element root cause:
- **Butt preview still imperfect:** as the item nears objects on ANOTHER layer, the landing preview to the
  left/right of those objects isn't accurate ("room to improve"). Confirms E2 (target outline) isn't running
  the drop resolver at the finger's X.
- **Preview leaks into the wrong row:** while moving the purple item, a preview of it also appears "down in
  the bottom row where it really shouldn't be." The target-outline element is drawing on a row it isn't
  actually targeting — another face of the two-half-objects bug. The single-proxy rewrite must draw the
  moving object on exactly ONE row (the hovered target) and nowhere else.

## 🐞 NEW BUG (2026-07-05 eve, real phone) — IMAGE LAYER ITEM RENDERS AS TWO ELEMENTS (BINDING, next on layer-render files)
User: image layer shows "a fat bar that says image + a skinnier IMG bar attached above it." The SKINNY bar is
long-pressable (→ image-layer dialog) but NOT movable; the FAT bar is movable but has NO long-press dialog.
"These two things need to be combined into one element."
HYPOTHESIS (verify): the image-as-layer item is a `TextOverlayItem` (imageUri) with a layerId. It is being
drawn by BOTH rendering subsystems simultaneously:
  - E-fat = the LEGACY text-overlay timeline lane (pre-M6 rendering) — movable, no layer dialog.
  - E-skinny = the new `LayerRowRenderer` layer row — long-press → showLayerItemActionsDialog, but move goes
    through the (broken) layer-drag path.
FIX: an overlay item that belongs to a layer track (has a non-null/real layerId, i.e. appears in
`Timeline.getLayers()`) must render ONLY in its LayerRowRenderer row and be SUPPRESSED from the legacy
text-overlay lane — so there is ONE element that is both movable AND long-pressable with the layer dialog.
Find where the legacy lane iterates overlays (EditorTimelineView) and exclude layer-assigned ones (mirror
how LayerPreviewController.visibleTextOverlays already filters for PREVIEW — do the timeline-render equivalent).
This shares files with the split-element drag rewrite (LayerRowRenderer/EditorTimelineView) → do it in the
SAME sequential pass (fold into the drag agent's follow-up; never a parallel agent on these files).

## ⤴️ USER RE-CONFIRMED (drag): can't drag an item UP to a NEW TOP layer (only down); drag "keeps trying to
CREATE layers instead of snapping between / to extreme top+bottom." = the above-top new-layer zone (A13) +
the unified gap-insertion model (slice 2 blueprint), still owed. WORKAROUND NOW LIVE: the P2 "New layer
above" button (move-clip dialog, long-press the item) does exactly this reliably — point the user to it.

## 🔧 IN FLIGHT / QUEUED
- **"New layer above and below all other layers"** — user couldn't do it. Being built NOW as the reliable
  button path (move-clip dialog "New layer above/below"). This is the top unblocker; verify on device.
- After reliable path: the split-element drag rewrite (device-in-the-loop, per the above symptoms).
