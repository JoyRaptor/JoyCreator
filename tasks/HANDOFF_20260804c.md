# Handoff — 2026-08-04 (supersedes NEXT_SESSION_PROMPT_20260804b.md)

FadCam / Joy Creator — `C:\+Projects\Screenrecorder\FadCam`, branch `joy-creator`.
Read `tasks/LEDGER.md` FIRST. Never delete a ledger entry — strike through and correct in place.

**JoyRaptor is not a developer.** Make the ENGINEERING calls yourself; bring him PRODUCT/UX decisions
only. He cannot check the work, which is a reason to be MORE rigorous, not less.

---

## ⚠ READ THIS FIRST — the install watcher is now DEAD, on purpose

The repo had `gradlew.bat -t installDefaultDebug` running as a background continuous build. **Every
successful compile auto-installed onto whatever phone was plugged in.** On 2026-08-04 that pushed a
build onto JoyRaptor's **Note 20 — the phone holding his real 45-minute project** — without anyone
deciding to. It was stopped (the wrapper `cmd.exe` plus the Gradle daemons).

Consequence: **`adb install` is now manual.** If a build "doesn't take", that is why.

```bash
adb -s SANDBOX_SERIAL install -r app/build/outputs/apk/default/debug/app-default-arm64-v8a-debug.apk
```

Restarting the watcher is a bad idea while the Note 20 is ever attached. If you want it back, gate
it on the sandbox serial.

## THE THREE LESSONS (the third is new and cost the most)

1. **A green harness is not a working feature.** M11 anchoring shipped with 35 passing checks, a
   debug probe and a confident commit message — and was **completely inert**. Ask "who CALLS this?"
2. **Adversarially review your own fixes.** Five rounds found ~45 defects. Round five found a
   HIGH-severity regression in round four's *fix*.
3. **A fix can be worse than the bug, and the smoke test won't tell you.** The reorder playhead
   restore passed a fraction of the *effective* segment where the consumer wanted a fraction of the
   *full source*. On an untrimmed 1× clip those are identical — so it looked perfect in exactly the
   test a fresh build gets. On any trimmed clip it threw the player to the clip's end. **When two
   quantities coincide on the default case, the default case cannot test them.**

## STATE — all committed, tree clean, five harnesses green

`bash tools/jvm-harness/run-anchor.sh` (35) · `run-promote.sh` (24) · `run-matte.sh` (14) ·
`java -cp tools/jvm-harness/out-anchor AnchorMathTest` (39) ·
`java -cp tools/jvm-harness/out5 UndoManagerTest` (46)

Recompile the undo one after editing it (there is no run-undo.sh):
```bash
javac -nowarn -d tools/jvm-harness/out5 -sourcepath "tools/jvm-harness/stubs;app/src/main/java;tools/jvm-harness" tools/jvm-harness/UndoManagerTest.java
```

### Shipped and device-proven
- **§2d** overlays no longer drift past a transition (5.50s → 4.90s, measured).
- **§2e** image clips no longer kill the export; fixed in the PREVIEW too (`util/ImageMime`).
- **§3i M12** clips move spine ⇄ layer via the Move drawer ↑/↓. Proven from `project.json`.
- **M11 anchoring** proven end to end: attach on creation → persist → load → shift → save.
- **Tap-to-seek latency — CONFIRMED FIXED BY JOYRAPTOR** on the Note 20: "it feels much faster."
  Cause was a plain tap running the frame-accurate *settle* built for drag-scrubbing.
- **Doors** on: text outline/glow/shadow/plate, PiP Mask, PiP blend mode, orphan-anchor policy.
- **Caption picker** is opt-in.

### Built, NOT device-verified — see `HANDTEST_20260804.md`
`adb`'s `input swipe` interpolates movement over the whole gesture, so it **cannot express
hold-still-THEN-move**. These need fingers on glass. Do not grind synthetic input at them.

1. The dislodge (hold → purple halo + chevron → pull up) **and 1b, the new hand-over**.
2. Mask dialog → BACK → no hole left.
3. Text style round-trip.

## WHERE THE DRAG WORK ACTUALLY STANDS

JoyRaptor's ask, verbatim: *"the remaining things required for cleanly dragging clips onto the main and
off. Lift off animation and highlighted area where they're going to be inserted so that everything
is cleanly projected [to] the user so that they don't have to guess."*

- **DONE — the lift.** Purple halo + up-chevron on an armed clip (`drawDislodgeArmedLift`).
- **DONE — the hand-over** (`0ebb39b`). The dislodged clip is now an ordinary picked-up layer item
  via `LayerGestureController.adoptDrag()`, so it follows the finger and inherits magnet
  suppression, WYSIWYG drop, edge auto-pan, minimap nav, the overlap resolver and the undo merge.
  **Needs hand test 1b.**
- **DONE — the insertion highlight, seam-only** (`449a9b3`). A held layer clip over the master
  track draws a solid purple line at the seam it will insert at, and the release lands there
  (`promoteToMaster` already took an index). Payload legality is enforced BEFORE drawing, so text
  is never offered a spot the drop would refuse. **Needs hand test 1c.**
- **DONE — one gesture, one undo** (`4caa631`). The dislodge recorded two actions (demote, then
  drop); `UndoManager.mergeNextIntoTop()` folds them. 6 new harness checks.
- **NOT DONE — the CUT state (§3A.4).** Dropping mid-clip should show **red + scissors** and split
  the clip underneath. Deferred on purpose: there is no split-and-insert model op, and drawing a
  scissors the release would not honour is worse than not drawing it. Every hover currently snaps
  to a seam, so the indicator is always truthful. This is the next piece of drag work.

## STILL OPEN

- **Chroma key has no UI.** Deliberate — the key is export-only and a tolerance slider tuned blind
  is unacceptable. Needs preview parity, which needs the GL compositor.
- **Degenerate skipped items** (`MIN_EXPORT_SEGMENT_MS`) compress the composition by more than
  `Σ effectiveTransitionMs`, so `editorTimeOffsetFor` is a MODEL of the cursor, not the cursor.
  Exact fix: have `buildComposition` record real dropped-content per clip.
- **`FaditorEditorActivity` is ~900 lines heavier this session** and was already enormous. The mask
  dialog and decoration controls should become their own classes, matching `ObjectMenuSheet` /
  `FilterBottomSheet`. Do it when nothing else is mid-flight.
- The stacked-PiP-bands render after two "Move PiP" actions — captured, unexplained, uninvestigated.

## HOW TO WORK HERE

- **`adb -s SANDBOX_SERIAL`** always — a stale offline `emulator-5584` broke bare commands.
- **Device rule: Note 9 `SANDBOX_SERIAL` is the sandbox.** If the Note 20 `REAL_SERIAL` is
  attached, STOP all device work — it holds the real project.
- **`am start -n com.fadcam.beta/com.fadcam.SplashActivity`, NEVER `monkey`** (it calls
  `thawRotation()` and kills JoyRaptor's rotation lock).
- Back up `project.json` device-locally and restore with `run-as cp` — **never `adb push` onto it**
  (a failed push truncates it).
- **`export MSYS_NO_PATHCONV=1`** before `adb shell /storage/...` — but it breaks the harness
  scripts' `mktemp`. Run harnesses in a clean shell.
- **NEVER build while an export is running.**
- **`strings` is not installed.** Dex-scan with `grep -alc`, ALWAYS with a negative control — that
  is what caught a scan silently measuring nothing.
- **Exporting bumps `lastModified` and RE-SORTS the project list.** Identify projects by the logcat
  `Editor loaded saved project:` line, never by row position.
- **Live-writing dialogs need `setOnDismissListener` AND a save on revert** — BACK bypasses the
  button, and the 3s autosave debounce means a cancelled edit is often already on disk.
- **Verify a claim about a build by comparing binaries, not timestamps.** I told JoyRaptor his phone had
  a bug based on install time vs commit time; `md5sum` on the device APK vs the local one showed it
  already had the fix. One command would have prevented a false alarm.

Sandbox `302da9ac` restored byte-exact (md5 `a74cd91c…`); rotation lock `0`.
