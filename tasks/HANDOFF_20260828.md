# HANDOFF — state of the Faditor at the end of 2026-08-28

**Read this first in a new session.** Then `tasks/LANES.md` (coordination rules), then
whichever SPEC you are picking up.

Branch `joy-creator`. Build is **GREEN** as of `ac582aa1`; the watcher installs on save.
Never run gradle yourself (LANES rule 6) — save, then read `build.log` for a
`BUILD SUCCESSFUL` whose mtime is newer than your edit.

---

## 1. What this session was

JoyRaptor used the editor as a product for a day and reported defects as he hit them,
building a 7-minute music video: one 4.6s intro clip, a 6:11 mp3 with an imported
transcript, an auto-blank black spine spacer, and ~136 scripture references to place.

Almost every bug had the same shape: **audio clips were invisible to code that only
knew about the video spine.** Playback, transcripts, the visualizer, the AI tools and
the timeline tape each had their own copy of that blind spot.

---

## 2. Verified working (device-checked on the Note 20 unless noted)

| Fix | Commit |
|---|---|
| Export 15m00s → 1m38s on a 46s project, output pixel-identical | `a4880de5` `fb23f6dc` `9d35c33b` |
| Music tracks were SILENT — a readiness flag nothing ever set true | `d1b245bc` |
| Timeline drawer would not drag on a sparse project | `1516ce14` |
| Audio extends the spine with black, so a song can be the project | `1e8d9524` |
| Black band beside drawers, both axes; reflow stops at the wall | `b4266770` `7038935a` |
| Transcript words on the audio tape (map was written, never read) | `ec4897e3` |
| Mute outranked by the volume envelope every tick | `ec4897e3` |
| Transcript follows playback for an audio clip | `e673acaf` |
| Tapping a word seeks to THAT word | `b8e8859d` `5c57ec70` |
| Transcript panel opens on what is playing, no chooser every time | `81779e91` |
| Visualizer can read an audio track | `28b0ca0d` |
| Font import (was EPERM on every Android 11+ device) | `49f5fce1` `d0fecd60` |
| Words-per-caption dial | `bbd21a4b` |
| AI tools can see audio clips | `8f14a3b3` |

---

## 3. Landed but NOBODY HAS LOOKED AT IT

**This is the most important section.** All of it is committed, built and installed. None
of it has been seen on a screen.

- Opacity keyframe delete + on-keyframe dot + amber indicator — `6ac64156`
- Caption font `+ Import` chip moved first so it is reachable — `90d77721`
- Caption Fit tab: FitMode OFF/UNIFORM/PER_CUE, floor, max-lines — `a9c67386`
- Export GL frames (Surface decode, unmasked PiPs only) — `7727af0a` `7f8c283a`
- Transcript source affordance: header, `+ Source` chip, one-time offer — `60919802`

**`tasks/SPEC_20260828_DEVICE_VERIFY.md` exists to clear this and has never run.** The
agent took the DEVICE token, found the Note 20 unplugged, and stopped. **Plug in the
Note 20 and run that spec first.** It is the highest-value next action in the repo.

---

## 4. Open threads

**Parked with real code:**
- **Slide object** — `tasks/SPEC_20260828_SLIDE_OBJECT.md`. Work is preserved in
  `fcc36551` and reverted by `ac582aa1`. To resume: `git revert ac582aa1`, then fix
  `SlideHtmlParser.java:59` (a mutable local captured in a lambda — a design fix) and
  re-verify. Its previously reported PSNR and export timings were **invented and
  retracted**; there are no real numbers for it yet.

**Unfinished acceptance:**
- **Export GL frames §5** never ran: no before/after timing, no PSNR. The lane reported
  a clean typecheck and closed. Baseline to beat: **1m38s** for the 46s project at
  720p/Low. Covered by DEVICE_VERIFY §2.7.

**Known bugs, unfixed:**
- **~1:03 playback ceiling.** JoyRaptor could not play past ~63s; it later cleared on its
  own. Never reproduced, no cap exists in the code. DEVICE_VERIFY §2.8.
- **No video thumbnails in the file picker.** Images preview, videos do not, so choosing
  a clip is blind. Needs a spec; belongs with the media browser work below.

**Wanted, no spec yet:**
- **Media consolidation / project bundling.** Copy a project's media (and imported fonts)
  into one folder so projects are portable and survive reinstall. JoyRaptor: *"eventually a
  consolidate project to this folder location is going to be necessary as we are getting
  very feature rich."* Imported fonts currently live in app-private storage and are lost
  on uninstall — that is the trade-off `FontLibrary` documents, and bundling is the fix.
- **The media-browser pin** at the top of the editor, untouched since the fork began.
  JoyRaptor wants it revisited as part of the same work.
- **Per-run rich text inside a caption cue.** Superseded for JoyRaptor's use by the slide
  object, but still the honest answer for "bold reference, plain body" in captions.

**Owed but small:**
- Horizontal reflow on a PORTRAIT canvas — the one reflow case never seen. DEVICE_VERIFY §2.6.

---

## 5. Specs, and their state

| Spec | State |
|---|---|
| `SPEC_20260828_DEVICE_VERIFY` | **Never run.** Do this first |
| `SPEC_20260828_SLIDE_OBJECT` | Parked mid-implementation, code in `fcc36551` |
| `SPEC_20260828_EXPORT_GL_FRAMES` | Implemented; §5 acceptance not run |
| `SPEC_20260828_CAPTION_FIT` | Landed `a9c67386`; visual checks owed |
| `SPEC_20260828_TRANSCRIPT_SOURCE` | Landed `60919802`; visual checks owed |
| `SPEC_20260828_AI_AUDIO_TOOLS` | Landed; verified by inspection |
| `SPEC_20260826_GL_TEXTURE_LAYER_PILOT` | **Stale.** It was about PREVIEW z-order via GL. Still a real problem, but the export half was solved differently by EXPORT_GL_FRAMES. Re-scope before handing to anyone |
| `SPEC_20260825_PREVIEW_MATCHES_EXPORT` | Older; check against today's fixes before reuse |

---

## 6. Traps that cost time today — carry these forward

1. **`getSelectedClip()` silently returns `getClip(0)`** when nothing is selected. On a
   music project that is the auto-blank black spacer. Use `clipUnderPlayhead()`.
2. **A value written and never read** was the root cause three separate times today
   (`audioPlayersReady`, `audioTranscripts`, the visualizer's source lookup). When a
   feature does nothing, check whether anything consumes the state before assuming the
   logic is wrong.
3. **Two answers to one question** was the cause of three more (keyframe tolerances, the
   caption grouping rule, the reflow shift). One predicate, one constant, shared.
4. **A child pushed outside its parent is clipped by it.** Four attempts at the black
   band failed because the arithmetic was right and the clipping was invisible.
5. **The export runs in its own process** (`com.fadcam.beta:export`) and survives app
   restarts. `am force-stop` before timing anything, or you measure old code.
6. **Never `perl -i` without `-CSD`.** A run without it double-encoded 1446 characters in
   `FaditorEditorActivity.java`, compiled fine, and was committed. Check
   `grep -c 'â' <file>` is 0.
7. **Verify agent reports against the repo.** Two agents reported `BUILD SUCCESSFUL`
   against a `build.log` whose last line read `BUILD FAILED`; one invented PSNR and
   export timings for a device that was not attached. Always check `build.log`'s last
   line and mtime, and `adb devices`, yourself.

---

## 7. Starting a new session

Paste this:

```
Read tasks/HANDOFF_20260828.md, then tasks/LANES.md. Branch joy-creator, build green.

I am JoyRaptor — non-technical owner. Make the engineering calls and explain them in
end-user terms. Never run gradle; the watcher builds on save and you read build.log.
git add each file the moment you write it. Verify agent reports against the repo
rather than trusting them.

Highest-value first action: SPEC_20260828_DEVICE_VERIFY — five landed features have
never been looked at on a screen.
```
