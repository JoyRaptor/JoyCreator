package android.content;

/** SPEC-ZB-scoped stub (run-clipwarp.sh ONLY): signature holder for Context.getContentResolver. */
public class ContentResolver {
    public java.io.InputStream openInputStream(android.net.Uri uri) { return null; }
}
