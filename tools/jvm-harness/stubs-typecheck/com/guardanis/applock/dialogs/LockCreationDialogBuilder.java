package com.guardanis.applock.dialogs;

import android.app.Activity;

/** Compile-only stub — see {@link com.guardanis.applock.AppLock}. */
public class LockCreationDialogBuilder {
    public LockCreationDialogBuilder(Activity activity) {}
    public LockCreationDialogBuilder onLockCreated(Runnable r) { return this; }
    public LockCreationDialogBuilder onCanceled(Runnable r) { return this; }
    public Object show() { return null; }
}
